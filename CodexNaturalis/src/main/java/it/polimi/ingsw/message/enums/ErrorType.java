package it.polimi.ingsw.message.enums;

public enum ErrorType {
    CREDENTIAL_WRONG,
    NICKNAME_ALREADY_EXIST,
    ACCOUNT_ALREADY_LOGGED,
    INVALID_POSITION,
    INVALID_CARD_ID,
    INVALID_PERSONAL_GOAL,

}
