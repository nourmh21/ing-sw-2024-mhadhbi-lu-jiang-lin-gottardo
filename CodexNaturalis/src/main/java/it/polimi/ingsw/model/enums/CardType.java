package it.polimi.ingsw.model.enums;

/**
 * indicates the type of the card. It can be RESOURCE, GOLD, INITIAL, OBJECTIVE
 */
public enum CardType {
    RESOURCE,
    GOLD,
    INITIAL,
    OBJECTIVE
}
